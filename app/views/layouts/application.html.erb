<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnbPostwagon</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Molle&display=swap" rel="stylesheet">
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
  </head>
  <body>
    <%= yield %>
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        for (let i = 0; i < 20; i++) {
          const rose = document.createElement("div");
          rose.className = "rose";
          rose.textContent = "🌹";

          // Position gauche ou droite aléatoire
          const side = Math.random() > 0.5 ? 'left' : 'right';
          const offset = Math.random() * 50; // pour varier l'espace entre les roses

          rose.style[side] = `${offset}px`;
          rose.style.animationDuration = `${4 + Math.random() * 4}s`;
          rose.style.fontSize = `${24 + Math.random() * 16}px`;

          document.body.appendChild(rose);
        }
      });
    </script>
  </body>
</html>
